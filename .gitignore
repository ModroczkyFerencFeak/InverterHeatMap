# Geocoding log (geo.py futás hibái)
geo.log
# Python cache
__pycache__/

# Érzékeny környezet (backend/frontend titkok)
.env
.env.*
# Push védelem: tárolt jelszó hash (ne kerüljön gitbe)
.push-auth
# Belépő a statikus oldalhoz (Vercel: build generálja; helyben: setup-web-auth.ps1)
auth-config.js
